specVersion: 1.0.0
indexerHints:
  prune: auto
schema:
  file: ./schema.graphql
dataSources:
  - kind: ethereum
    name: LifeKeyProtocol
    network: base-sepolia
    source:
      address: "0x7e2824fF423A5215C58b7E8d89e37cDD13Ba0C39"
      abi: LifeKeyProtocol
      startBlock: 31995304
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.7
      language: wasm/assemblyscript
      entities:
        - AssetsAdded
        - AssetsRemoved
        - BeneficiariesAdded
        - BeneficiariesRemoved
        - Initialized
        - LifeKeyCreated
        - LifeKeyDeleted
        - OwnershipTransferred
        - RecoveryApproved
        - RecoveryCancelled
        - RecoveryClaimed
        - RecoveryCompleted
        - RecoveryInitiated
        - Upgraded
      abis:
        - name: LifeKeyProtocol
          file: ./abis/LifeKeyProtocol.json
      eventHandlers:
        - event: AssetsAdded(indexed uint256,indexed address,indexed address)
          handler: handleAssetsAdded
        - event: AssetsRemoved(indexed uint256,indexed address,indexed address)
          handler: handleAssetsRemoved
        - event: BeneficiariesAdded(indexed uint256,indexed address,indexed address)
          handler: handleBeneficiariesAdded
        - event: BeneficiariesRemoved(indexed uint256,indexed address,indexed address)
          handler: handleBeneficiariesRemoved
        - event: Initialized(uint64)
          handler: handleInitialized
        - event: LifeKeyCreated(indexed uint256,indexed address)
          handler: handleLifeKeyCreated
        - event: LifeKeyDeleted(indexed uint256,indexed address)
          handler: handleLifeKeyDeleted
        - event: OwnershipTransferred(indexed address,indexed address)
          handler: handleOwnershipTransferred
        - event: RecoveryApproved(indexed uint256,indexed address,uint256)
          handler: handleRecoveryApproved
        - event: RecoveryCancelled(indexed uint256,indexed address)
          handler: handleRecoveryCancelled
        - event: RecoveryClaimed(indexed uint256,indexed address,indexed address)
          handler: handleRecoveryClaimed
        - event: RecoveryCompleted(indexed uint256,indexed address)
          handler: handleRecoveryCompleted
        - event: RecoveryInitiated(indexed uint256,indexed address,address)
          handler: handleRecoveryInitiated
        - event: Upgraded(indexed address)
          handler: handleUpgraded
      file: ./src/life-key-protocol.ts
